Get an instances.
---
parameters:
  - $ref: "#/components/parameters/model_uuid"
  - $ref: "#/components/parameters/instance_uuid"

security:
  bearerAuth: []

tags:
  - instances

responses:
  200:
    description: An instances.
    content:
      application/json:
        schema:
          type: array
          items:
            $ref: "#/components/schemas/Instance"