Delete a model.
---
parameters:
  - $ref: "#/components/parameters/model_uuid"

security:
  brearerAuth: []

tags:
  - models

responses:
  200:
    description: The model was deleted.
  400:
    description: The model could not be deleted.