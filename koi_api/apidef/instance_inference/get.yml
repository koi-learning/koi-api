Get the latest inference data.
---
parameters:
  - $ref: "#/components/parameters/model_uuid"
  - $ref: "#/components/parameters/instance_uuid"

security:
  bearerAuth: []

tags:
  - inference_data
  - instances

responses:
  200:
    description: the inference data.
    content:
      application/octet-stream:
        schema:
          type: string
          format: binary
  400:
    description: dfgjkdnfg